namespace OpenNos.Domain
{
    public enum EventActionType
    {
        XPRATE,
        DROPRATE,
        MESSAGE,
        DISPOSE,
        LOCK,
        UNSPAWN,
        SPAWNONLASTENTRY,
        CLOCK,
        SPAWN,
        DROPITEMS
    }
}